# Copy to android/keystore.properties (never commit that file).
# Keystore path is relative to android/app/
storeFile=upload.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=upload
keyPassword=YOUR_KEY_PASSWORD
